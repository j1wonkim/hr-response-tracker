# tests/fixtures/

Saved HTML snapshots used to test scraper parsers. Parsers are tested
against these fixtures only — never against live sites — so tests stay
fast, deterministic, and don't hammer real ministry/Amnesty servers.

One subdirectory per source (e.g. `amnesty/`, `us_state_dept/`, `china_mfa/`)
once adapters land.
