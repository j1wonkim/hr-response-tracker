# prompts/

All LLM prompts used by the pipeline (state-perpetrator filter, issue
classifier, event–statement matcher) live here as plain text files, not
inline in code, so they can be tuned and versioned independently. Prompt
wording changes should be logged in [DECISIONS.md](../DECISIONS.md).

Empty for now — prompts land alongside the pipeline stages that use them.
